class MyCalendar {
    map<int,int> mp;
public:
    MyCalendar() {
        
    }
    
    bool book(int startTime, int endTime) {
        mp[startTime]++;
        mp[endTime]--;
        int sum = 0;
        for(auto it:mp){
            sum += it.second;
            if(sum > 1) {
                mp[startTime]--;
                mp[endTime]++;
                return false;
            }
        }
        return true;
    }
};
